import { mockedAuthorsList as authorsList } from '../constants';

// export const findAuthorById = (id) => {
// 	const author = authorsList.find((author) => author.id === id);
// 	return author.name;
// };

// export const listAuthorsString = (ids) => {
// 	return ids.map((id) => findAuthorById(id)).join(', ');
// };
